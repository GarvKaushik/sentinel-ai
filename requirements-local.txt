# Full local / dev / CI dependency set.
#
# = the base (deploy) deps + the LOCAL embedding backend. sentence-transformers
# pulls in torch, which is why it is kept out of the light deploy image. Use this
# file for offline development or to run the local-backend retrieval path:
#
#     pip install -r requirements-local.txt
#
# The deploy image installs only requirements.txt and uses the hosted Jina
# backend (EMBEDDING_BACKEND=jina) instead.

-r requirements.txt

sentence-transformers==3.1.1
pytest==8.3.3
